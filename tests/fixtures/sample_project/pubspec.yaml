name: sample_project
description: A sample Dart project for mutation testing integration tests
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
