name: compile_error_project
description: Test project with code that will cause compile errors when mutated
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
